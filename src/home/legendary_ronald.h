#ifndef POKETCG_HOME_LEGENDARY_RONALD_H
#define POKETCG_HOME_LEGENDARY_RONALD_H

#include <stdint.h>

/* >>> factory AIDoTurn_LegendaryRonald */
typedef struct { uint8_t f; } AIDoTurn_LegendaryRonaldResult;
AIDoTurn_LegendaryRonaldResult AIDoTurn_LegendaryRonald(uint8_t a, uint8_t f, uint8_t b, uint8_t c, uint8_t d, uint8_t e, uint16_t hl);
/* <<< factory AIDoTurn_LegendaryRonald */
#endif /* POKETCG_HOME_LEGENDARY_RONALD_H */
