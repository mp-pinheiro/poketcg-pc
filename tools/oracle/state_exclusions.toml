# Only call-frame bytes are masked by default. All other state is observable.
[[exclusions]]
region = "wram"
start = "0xDC00"
end = "0xDCFF"
reason = "PyBoy oracle synthesized call frame and its stack"

# Timing registers are normalized only when the analyzer proves they are not
# explicitly accessed by the routine under test.
[[timing_registers]]
name = "rDIV"
address = "0xFF04"
[[timing_registers]]
name = "rTIMA"
address = "0xFF05"
[[timing_registers]]
name = "rSTAT"
address = "0xFF41"
[[timing_registers]]
name = "rLY"
address = "0xFF44"
