[[blocked]]
name = "_TossCoin"
reason = "tests/cases/coin_toss.py intentionally leaves the full interactive animation/RNG/serial routine unregistered; no primary oracle case exists"
unblock = "add at least one deterministic primary schema-2 case for _TossCoin and a terminating full implementation, or land a separately reviewed permanent transform for it and its callers"

[[blocked]]
name = "UnreferencedSaveSerialReturnAddress"
reason = "0-callsite dead code (poketcg/src/home/serial.asm:459) whose entire observable output is the raw entry stack pointer (ld hl, sp+-2; pop hl) captured into wSerialReturnSP/wSerialReturnAddress. The factory's own verification phases run two different oracle backends with two different entry-SP conventions for an isolated probed call: tools/oracle/gbref/compare_one.py (primary phase) enters with sp=0xFFFC and a pushed return address of 0xFEA0, while tests/test_leaves.py (diff phase, PyBoy-backed) enters with sp=0xCFBE and a pushed sentinel of 0xCFF0. A single C body can only reproduce one of the two SP values, so it necessarily fails whichever phase it does not target -- confirmed by direct diagnostic evidence from both phases in this cycle."
unblock = "add a project-wide abstraction that lets a ported routine query its own entry SP consistently across both oracle backends, or land a separately reviewed permanent transform that special-cases this routine (and any other sp-relative dead code) out of the dual-oracle comparison"

[[blocked]]
name = "AnimationCommand_AnimScreen"
reason = "poketcg/src/engine/duel/animations/commands.asm:151 calls PlayDuelAnimation, whose real asm (play_animation.asm:26-28, ldh a, [hBankROM]) captures the CALLER's ambient ROM bank and hands it, unmodified, into a `jp PlayAttackAnimationCommands_NextCommand` tail call that reuses that bank number as an opcode-dispatch index (cases 1-7 -> SendNextPrinterPacketByte, 8+/default -> plain de+1 arithmetic). tools/oracle/gbref (the primary-phase native harness) auto-forces hBankROM to the probed routine's OWN declared symbol bank (6, from mode:\"symbol\" mapper derivation) before every isolated call, regardless of any wram/hram seed a case supplies -- confirmed by seeding 0xFF80 to 0x00, 0x06, and 0x09 in three separate diagnostic runs and observing the native side always resolve played.a=6 (hits the printer branch) while the real-ROM reference consistently resolves a value outside 1-7 (hits the default branch). No case-level seed can override the harness's own forced value, so native and reference are structurally guaranteed to diverge for this routine."
unblock = "add a case-schema knob that lets a schema-2 case override the native harness's auto-forced hBankROM/rom_bank derivation independently of the mapper's rom_bank (needed for correct $4000-$7FFF paging), or land a separately reviewed permanent transform for PlayDuelAnimation's callers that models the bank-reuse trick directly instead of round-tripping through hBankROM"

[[blocked]]
name = "PracticeDuel_RepeatInstructions"
reason = "poketcg/src/engine/duel/core.asm:2710 calls PrintPracticeDuelDrMasonInstructions(FollowMyGuidancePracticeDuelText), a multi-page tutorial text box whose rendering loop spins through DoFrame/ReadJoypad (poketcg.sym bank 0, ~0x512-0x549) waiting for real per-frame joypad-interrupt-driven page advances. An isolated probed call supplies only one static input_events snapshot (the legacy case schema's `keys` field yields exactly one {\"keys\": N} event, tools/oracle/schema.py/_schema_migration.py), which cannot satisfy the repeated press/release edges this specific (long) text needs. Confirmed BUDGET_EXHAUSTED at cycle_budget 4,000,000 / 20,000,000 / 120,000,000 (30x range) with keys=0x00 and keys=0x01, PC pinned inside DoFrame/ReadJoypad every time -- a genuine infinite spin, not merely a slow completion. The sibling PrintPracticeDuelDrMasonInstructions case (hl=0x01DB, a shorter text) already lands fine with default budgets, confirming the harness's text-rendering machinery itself is not at fault; this specific text's page count exceeds what one static input snapshot can drive to completion."
unblock = "add multi-frame input_events sequencing to the legacy CASES schema (or write a schema-2 case directly overriding input_events with several {\"keys\": N} entries across simulated frames) so a probed call can advance a multi-page text box through its real edge-triggered continue prompts, or land a separately reviewed permanent transform for this call site"

[[blocked]]
name = "OverworldMap_HandleDPad"
reason = "poketcg/src/engine/overworld_map.asm:72 (bank 4, poketcg.sym 04:4e97) ends with pop bc / pop hl / ret after a leading push hl / pop hl no-op, a net +1-word stack imbalance relative to entry SP. Traced by hand and confirmed empirically (native probe, direct JSON invocation): the real calling convention requires the caller to have pushed two extra words [Y, X] before `call`, so this routine's own epilogue discards the harness's synthesized return address into bc, discards Y into hl, and treats X as the new `ret` target -- a computed-jump trick, not a bug. The schema-2 harness's single-frame call() (tools/oracle/pyboy_oracle.py) only ever pushes one sentinel return address at STACK_TOP-2 and explicitly forbids seeding the $CF00-$D000 RESERVED window that would hold any pre-pushed [Y, X], so a normal primary case can never supply them: the corrupted `ret` jumps to whatever pb.memory leaves at STACK_TOP+2..3 (zero after _reset_ram), which is not the SPIN sentinel and never fires the completion hook, timing out at MAX_FRAMES. The `completion: {\"mode\": \"pre-ret\", \"pc\": ...}` escape hatch (used successfully by CopyGfxData, ZeroRAM, DuelTransmissionError, all bank-0 routines) is not usable either: pyboy_oracle.py's `_arm()` always calls `hook_register(0, addr, ...)` with bank hardcoded to 0, and pyboy's hook_register is bank-specific, so a stop_pc inside this routine's own bank 4 (0x4eb9, the `.no_transition` label right before the corrupting pop/pop/ret) never fires -- confirmed the process never returns and must be killed after several minutes with the hook silently never triggering."
unblock = "add a `bank` field to the schema-2 completion dict (threaded through test_leaves.py/pyboy_oracle.py's `_arm`) so a pre-ret stop_pc can target a banked routine's own bank, or add a case-schema knob to pre-push extra stack words below the synthesized return address (and let RESERVED grow accordingly) so the real push/pop calling convention can be honored end to end, or land a separately reviewed permanent transform for this routine and any other stack-computed-jump idiom the disassembly still contains"

[[blocked]]
name = "InitScreenAnimation"
reason = "poketcg/src/engine/duel/animations/screen_effects.asm:4 dispatches through ScreenAnimationFunctions (a bus-data jump table: function pointer + duration per DUEL_SCREEN_ANIMS entry) via `call CallBC`, a `retbc` trampoline docs/phase1-transform.md marks for deletion in favor of a direct C function-pointer call once every table target is ported. The table covers 6 animations (ShakeScreenX_Small/Big, ShakeScreenY_Small/Big, WhiteFlashScreen, DistortScreen); only ShakeScreenX_Small/Big are landed per this basename's already-ported list. This is docs/port-contract.md's documented Reason 4 (\"Tail dispatch into unported engine\") verbatim -- the call-graph sweep cannot see the indirect CallBC edge, so progress.json reports this routine ready=true/blockers=[] while it is not portable until ShakeScreenY_Small, ShakeScreenY_Big, WhiteFlashScreen, and DistortScreen all land."
unblock = "port ShakeScreenY_Small, ShakeScreenY_Big, WhiteFlashScreen, and DistortScreen (the four remaining ScreenAnimationFunctions targets), then re-port InitScreenAnimation with CallBC collapsed into a direct C function-pointer table per docs/port-contract.md's leaf-slice resolution for CallBC/CallHL/CallHL2/JumpToFunctionInTable"

[[blocked]]
name = "DrawPlayerPortraitAndPrintNewGameText"
reason = "poketcg/src/engine/menus/start.asm:403 chains DisableLCD -> LoadConsolePaletteData -> InitMenuScreen -> EnableAndClearSpriteAnimations -> SetDoFrameFunction(HandleAllSpriteAnimations) -> farcall DrawPlayerPortrait -> FadeScreenFromWhite -> DoFrameIfLCDEnabled -> PrintScrollableText_NoTextBoxLabel -> ResetDoFrameFunction -> EnableAndClearSpriteAnimations, all against the real ROM (reference-side, independent of the candidate C body). Each callee is independently green in isolation (DrawPortrait, InitMenuScreen/Func_1d705, FadeScreenFromWhite with its own established LCDC=0/VBLANK=0xFE seed all pass with their own established budgets), but the full chain does not terminate in this harness: with the FadeScreenFromWhite-precedent seed (LCDC=0, VBLANK=0xFE) the reference PC pins at poketcg.sym 00:ffd6 (unlabeled HRAM past hffb7, consistent with a DMA/VBlank-interrupt trampoline) across both an 8,000,000/32,000,000 and a 30,000,000/120,000,000 instruction/cycle budget -- identical PC at both budgets confirms genuine non-termination, not slowness. Reseeding VBLANK=0x00 moves the reference PC to 0x4642 with SP=0x9235 (inside VRAM address space), which is not a legitimate stack pointer and indicates the reference diverges into unrelated/corrupted execution rather than progressing -- a worse, not better, outcome. This matches docs/port-contract.md's DoFrame/VBlank-interrupt class already documented in tests/cases/frames.py (`DoFrame` LCD-on case, oracle=False) and tests/cases/lcd_enable_frame.py (`EnableLCD` LCD-on case, oracle=False): a single isolated probed call cannot resolve a real VBlank interrupt fired mid-routine. Unlike those single-callee cases, this routine has no non-hanging branch (unconditional straight-line sequence), so no primary schema-2 case can be constructed at all, unlike ChallengeMachine_ShowNewRecord (this cycle) which had a testable early-return branch."
unblock = "add multi-frame VBlank/interrupt simulation to the schema-2 harness so a chain of several real interrupt-driven calls (InitMenuScreen + FadeScreenFromWhite + DoFrame + text-advance) can complete against the reference ROM in one probed call, or land a separately reviewed permanent transform for this routine that verifies it via decomposed, already-green callee contracts instead of one end-to-end oracle run"

[[blocked]]
name = "Script_Nikki"
reason = "poketcg/src/scripts/grass_club.asm:95 falls through, after its wCurMap prelude branch, into `start_script` (rst $20) -- docs/vision.md Phase 6: RST20 (poketcg/src/engine/overworld/scripting.asm:549) pops its own return address as a bytecode pointer into wScriptPointer and loops an already-ported ScriptCommand_* dispatcher (RunOverworldScript) until wBreakScriptLoop, then `retbc` resumes native code. That interpreter entry point (RunOverworldScript/RunScript) is not exposed as a callable C function anywhere in src/home -- confirmed by grep across the whole ported tree. Script_Nikki's own bytecode falls through into Script_BeatNikki, itself another start_script bytecode blob (a bus-data label with no C prototype per this packet's own callee list). This is not novel to Script_Nikki: nine other start_script routines in this factory have already been driven to generation=8 (the retry ceiling) and left in the `red` state with the identical recurring diagnostic `unsupported-evidence: no primary oracle case` -- Script_Torch, Script_Clerk10, Script_GiftCenterClerk, Script_LegendaryCardTopLeft, Script_LegendaryCardTopRight, Script_LegendaryCardBottomLeft, Script_LegendaryCardBottomRight, Script_LegendaryCardLeftSpark, Script_Woman2 (verified directly from their attempts/*/result.json). No candidate across any of those ~200 generated attempts has ever constructed a primary schema-2 case, because the bytecode body is not sequential C statements the oracle can single-step -- it is data consumed by an interpreter this factory has not exposed as a leaf callee."
unblock = "port RunOverworldScript/the RST20 dispatch loop itself as a callable C function (e.g. `void RunScript(uint16_t hl)`) so a script routine's C body can call it against its own embedded bytecode blob and be verified end-to-end, or land a separately reviewed permanent transform that represents `start_script` bodies as data blobs plus one shared RunScript entry point per docs/vision.md Phase 6, rather than issuing them as individual per-routine factory attempts"

[[blocked]]
name = "Script_Tech1"
reason = "poketcg/src/scripts/mason_laboratory.asm:58 counts owned energy cards via EnergyCardList (a legitimate gb_read8 data label, representable in C), then BOTH of its outcome branches (`low_on_energies` and the not-low-energy path) fall into `start_script` (rst $20) bytecode -- the same RunOverworldScript/RST20 interpreter class documented on Script_Nikki above. That interpreter entry point is still not exposed as a callable C function anywhere in src/home. Unlike Script_Nikki, Script_Tech1's PRE-start_script prelude (the energy-counting loop) is itself fully portable and testable, but the routine as issued (one factory attempt, one C function) cannot terminate in either bytecode branch without RunScript, so no primary schema-2 case can cover the routine's actual exit behavior."
unblock = "same as Script_Nikki: port RunOverworldScript/the RST20 dispatch loop as a callable C function, or land a separately reviewed permanent transform for start_script-bodied routines; once available, Script_Tech1's own energy-counting prelude is straightforward to port alongside it"
