
[[blocked]]
name = "SetUpBossStartingHandAndDeck"
reason = "oracle timeout: routine never returns under synthesized call"
unblock = "escalation: derive setup prelude or record dependency"

[[blocked]]
name = "_CopyCardNameAndLevel_HalfwidthText"
reason = "oracle timeout: text-engine spin, salvaged cases lack setup prelude"
unblock = "escalation: author SetupText prelude cases"

[[blocked]]
name = "Func_fcad"
reason = "oracle timeout under synthesized call"
unblock = "escalation: derive setup prelude or record dependency"

[[blocked]]
name = "Script_Tech1"
reason = "script engine call never returns (issue 24 evidence)"
unblock = "port script-VM interpreter core"

[[blocked]]
name = "Script_Specs2"
reason = "script engine call never returns (issue 52 evidence)"
unblock = "port script-VM interpreter core"

[[blocked]]
name = "DoPracticeDuelAction"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "AddCardIDToVisibleList"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "CalculateBDividedByA_Bank8"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "Func_2c6d9"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "PrintCardListHeaderAndInfoBoxTexts"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "DisplayCardPage"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "PrintPracticeDuelLetsPlayTheGame"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "Script_Nikki"
reason = "script engine call never returns (issue 39 evidence): both branches enter RST20/RunOverworldScript; direct Oracle.call did not return in 240 frames"
unblock = "port script-VM interpreter core"

[[blocked]]
name = "PrinterMenu_QuitPrint"
reason = "trampoline-direct-call (issue 65 evidence): `add sp,$2` at printer.asm:262 discards its own return address and returns through HandlePrinterMenu's caller frame; the oracle synthesizes only one frame, so standalone calls time out (confirmed all-zero and poisoned)"
unblock = "inline at its two call sites once HandlePrinterMenu is ported"

[[blocked]]
name = "SendPrinterPacket"
reason = "interrupt-driven busy-wait (issue 22 evidence): spins on wPrinterPacketSequence, only advanced by the serial ISR; the oracle clears rIE before every call and the port has no ISR, so it never returns"
unblock = "model the serial handshake or dissolve per the Phase-1 transform"

[[blocked]]
name = "UnreferencedSaveSerialReturnAddress"
reason = "dissolved by Phase-1 transform (issue 28 evidence): captures ambient sp/pc, which ProbeState cannot represent; the only byte-exact body would hardcode harness-private frame constants. Dead code, zero callsites"
unblock = "none needed - delete with the hardware transform"

[[blocked]]
name = "UnreferencedGoToSerialReturnAddress"
reason = "tail dispatch into unported engine (issue 28 evidence): overwrites sp from wSerialReturnSP and jumps to a caller-supplied WRAM address; no GB address exists to jump to in C. Dead code, zero callsites"
unblock = "none needed - delete with the hardware transform"

[[blocked]]
name = "AIDoAction"
reason = "tail dispatch into unported engine: every live path (c in 1..5) reaches JumpToFunctionInTable -> DeckAIPointerTable, none of whose AIActionTable_* targets are ported; the c==0 CopyDeckData branch is dead"
unblock = "becomes a C function-pointer table once the AI action handlers land"

[[blocked]]
name = "SendSGB"
reason = "SGB path, dropped by Phase 1: bit-bangs rJOYP to transmit SNES-side packets; all 31 callsites are SGB packet transmission. docs/vision.md scopes the SGB path as drop-or-flag"
unblock = "none needed unless SGB support is added behind a flag"

[[blocked]]
name = "BankpushROM"
reason = "return-frame manipulation: relocates the return address via sp-relative addressing with a net non-zero SP delta (-2); ProbeState models no stack pointer and the two oracles place frames at different absolute addresses"
unblock = "delete with the Phase-1 transform, as SwitchToBankAtSP was"

[[blocked]]
name = "BankpushROM2"
reason = "return-frame manipulation: same net -2 SP delta as BankpushROM"
unblock = "delete with the Phase-1 transform, as SwitchToBankAtSP was"

[[blocked]]
name = "BankpopROM"
reason = "return-frame manipulation: consumes the smuggled word, net +2 SP delta"
unblock = "delete with the Phase-1 transform, as SwitchToBankAtSP was"

[[blocked]]
name = "_ExecuteGameEvent"
reason = "tail dispatch into unported engine: GameEventPointerTable has 8 slots and only GameEvent_Overworld is ported; JumpToFunctionInTable is itself excluded. Spilled from map-L42, whose HandleMapWarp landed"
unblock = "becomes an 8-entry C function-pointer table once the GameEvent_* handlers land"

[[blocked]]
name = "RunOverworldScript"
reason = "tail dispatch into unported engine: OverworldScriptTable has 105 entries and zero ScriptCommand_* handlers are ported, so no jp hl target exists in C. Spilled from script-L108, whose other two routines landed"
unblock = "port ScriptCommand_* handlers in scripting.asm, then build the dispatch table"

[[blocked]]
name = "DrawPlayerPortraitAndPrintNewGameText"
reason = "callee still unported: farcall DrawPlayerPortrait -> DrawPortrait -> _DrawPortrait (scenes.asm); the scenes basename is not registered at all. Every other callee resolves"
unblock = "port scenes.asm _DrawPortrait, then DrawPlayerPortrait/DrawPortrait"

[[blocked]]
name = "Func_3bb5"
reason = "tail dispatch into unported engine: hl is supplied entirely by the unported screen_effects.asm Func_1ce03; the 8 candidate targets (SetScreenForDuelAnimation, PrintDamageText, UpdateMainSceneHUD, DuelAnim153-157) have no C symbols, and the DUEL_ANIM_158_UNUSED path uses wDuelAnimDamage bytes as a raw code pointer"
unblock = "port the screen_effects.asm targets, then dissolve the dispatch into Func_1ce03"

[[blocked]]
name = "TryExecuteEffectCommandFunction"
reason = "tail dispatch into unported engine: dispatches through EffectCommands (632 dbw entries, 569 distinct targets); only 8/569 (1.4%) have landed C symbols, so no complete function-pointer table can be built"
unblock = "land substantially more effect_functions targets, then build the dispatch table"

[[blocked]]
name = "LoadMapTilesAndPals"
reason = "SGB path has observable memory effects the port drops: on wConsole==CONSOLE_SGB the oracle runs SetSGB2AndSGB3MapPalette (DecompressSGBPalette writes WRAM before SendSGB transmits), so any SGB-console case diverges by construction. Separately the CGB/DMG palette output still differs after the LoadBGPalette fix. Port drafted in lane-2; the probe/PyBoy bank-entry asymmetry it originally hit is now fixed via the opt-in rom_bank case key"
unblock = "decide SGB-case policy (drop the case or mark oracle:False with asm-derived expectations), then finish diffing the CGB/DMG palette path"

[[blocked]]
name = "PracticeDuel_PlayGoldeen"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "HandleFailedToContinueDuel"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "PrintPracticeDuelDrMasonInstructions"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "PracticeDuel_RepeatInstructions"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "AIMakeDecision"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "Func_6ba2"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "DecideLinkDuelVariables"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "PrintAttachedEnergyToPokemon"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "HandleYesOrNoMenu"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "DrawTextBox_PrintTextNoDelay"
reason = "pre-factory definition already in src .c without a factory block; re-porting redefinitions"
unblock = "backfill CONTRACT cases for the hand-landed definition, then remove from blocked.toml"

[[blocked]]
name = "ExecuteNextSFXCommand"
reason = "pre-factory definition already in src .c without a factory block; re-porting redefinitions"
unblock = "backfill CONTRACT cases for the hand-landed definition, then remove from blocked.toml"

[[blocked]]
name = "Func_fc1cd"
reason = "pre-factory definition already in src .c without a factory block; re-porting redefinitions"
unblock = "backfill CONTRACT cases for the hand-landed definition, then remove from blocked.toml"

[[blocked]]
name = "Func_fc26c"
reason = "pre-factory definition already in src .c without a factory block; re-porting redefinitions"
unblock = "backfill CONTRACT cases for the hand-landed definition, then remove from blocked.toml"

[[blocked]]
name = "Func_fc279"
reason = "pre-factory definition already in src .c without a factory block; re-porting redefinitions"
unblock = "backfill CONTRACT cases for the hand-landed definition, then remove from blocked.toml"

[[blocked]]
name = "Music1_end"
reason = "pre-factory definition already in src .c without a factory block; re-porting redefinitions"
unblock = "backfill CONTRACT cases for the hand-landed definition, then remove from blocked.toml"

[[blocked]]
name = "Music1_note"
reason = "pre-factory definition already in src .c without a factory block; re-porting redefinitions"
unblock = "backfill CONTRACT cases for the hand-landed definition, then remove from blocked.toml"

[[blocked]]
name = "Music2_end"
reason = "pre-factory definition already in src .c without a factory block; re-porting redefinitions"
unblock = "backfill CONTRACT cases for the hand-landed definition, then remove from blocked.toml"

[[blocked]]
name = "Music2_note"
reason = "pre-factory definition already in src .c without a factory block; re-porting redefinitions"
unblock = "backfill CONTRACT cases for the hand-landed definition, then remove from blocked.toml"

[[blocked]]
name = "SFX_ApplyPitchOffset"
reason = "pre-factory definition already in src .c without a factory block; re-porting redefinitions"
unblock = "backfill CONTRACT cases for the hand-landed definition, then remove from blocked.toml"

[[blocked]]
name = "Script_BeatAaron"
reason = "pre-factory definition already in src .c without a factory block; re-porting redefinitions"
unblock = "backfill CONTRACT cases for the hand-landed definition, then remove from blocked.toml"

[[blocked]]
name = "DecompressPermissionMap"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "DrawWholeScreenTextBox"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "AIPlay_PokemonTrader"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "CreditsSequenceCmd_PrintTextBox"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "AIPlay_PokemonCenter"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "DrawPlayArea_HandText"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "CloseIRCommunications"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "PrintPlayersCardsText"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "Func_c268"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "AIPlay_PokemonFlute"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "AIPlay_Switch"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "SetDeckMachineTitleText"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "Shift_ChangeColorEffect"
reason = "oracle timeout: callee never returns"
unblock = "port the blocking callee (see verdict)"

[[blocked]]
name = "PrintDeckNameFromInput"
reason = "callee has no C symbol: DrawDeckNamingScreenBG"
unblock = "port or transform the named callee"

[[blocked]]
name = "DrawDeckNamingScreenBG"
reason = "callee has no C symbol: PrintDeckNameFromInput"
unblock = "port or transform the named callee"
